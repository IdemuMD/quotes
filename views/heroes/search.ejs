<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Search Heroes</title>
    <style>
        body { font-family: Arial, sans-serif; margin: 20px; }
        h1 { color: #333; }
        form { margin-bottom: 20px; }
        input { padding: 5px; }
        button { padding: 5px 10px; background: #007bff; color: white; border: none; cursor: pointer; }
        button:hover { background: #0056b3; }
        .hero { border: 1px solid #ddd; padding: 10px; margin: 10px 0; }
        .hero h3 { margin: 0; }
        .actions { margin-top: 10px; }
        a { text-decoration: none; color: #007bff; }
        a:hover { text-decoration: underline; }
    </style>
</head>
<body>
    <h1>Search Heroes</h1>
    <a href="/heroes">Back to All Heroes</a>
    <form action="/heroes/search" method="GET">
        <input type="text" name="q" placeholder="Search by alter ego, real name, or citizenship" value="<%= typeof query !== 'undefined' ? query : '' %>" required>
        <button type="submit">Search</button>
    </form>
    <% if (heroes && heroes.length > 0) { %>
        <h2>Results for "<%= query %>"</h2>
        <% heroes.forEach(hero => { %>
            <div class="hero">
                <h3><%= hero.alterEgo %></h3>
                <p><strong>Real Name:</strong> <%= hero.realName %></p>
                <p><strong>Alignment:</strong> <%= hero.alignment %></p>
                <p><strong>Citizenship:</strong> <%= hero.citizenship %></p>
                <div class="actions">
                    <a href="/heroes/<%= hero._id %>">View Details</a> |
                    <a href="/heroes/<%= hero._id %>/edit">Edit</a> |
                    <form action="/heroes/<%= hero._id %>?_method=DELETE" method="POST" style="display:inline;">
                        <button type="submit" onclick="return confirm('Are you sure?')">Delete</button>
                    </form>
                </div>
            </div>
        <% }) %>
    <% } else if (typeof query !== 'undefined') { %>
        <p>No heroes found for "<%= query %>".</p>
    <% } %>
</body>
</html>

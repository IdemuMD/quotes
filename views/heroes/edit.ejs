<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Hero</title>
    <style>
        body { font-family: Arial, sans-serif; margin: 20px; }
        h1 { color: #333; }
        form { max-width: 400px; }
        label { display: block; margin-top: 10px; }
        input, select, textarea { width: 100%; padding: 5px; }
        button { margin-top: 10px; padding: 10px; background: #007bff; color: white; border: none; cursor: pointer; }
        button:hover { background: #0056b3; }
        .error { color: red; }
    </style>
</head>
<body>
    <h1>Edit Hero: <%= hero.alterEgo %></h1>
    <a href="/heroes/<%= hero._id %>">Back to Hero</a>
    <% if (typeof error !== 'undefined') { %>
        <p class="error"><%= error %></p>
    <% } %>
    <form action="/heroes/<%= hero._id %>?_method=PUT" method="POST">
        <label for="alterEgo">Alter Ego:</label>
        <input type="text" id="alterEgo" name="alterEgo" value="<%= hero.alterEgo %>" required>

        <label for="realName">Real Name:</label>
        <input type="text" id="realName" name="realName" value="<%= hero.realName %>" required>

        <label for="age">Age:</label>
        <input type="number" id="age" name="age" value="<%= hero.age %>" required>

        <label for="birthYear">Birth Year:</label>
        <input type="number" id="birthYear" name="birthYear" value="<%= hero.birthYear %>" required>

        <label for="fullTimeHero">Full Time Hero:</label>
        <select id="fullTimeHero" name="fullTimeHero" required>
            <option value="true" <%= hero.fullTimeHero ? 'selected' : '' %>>Yes</option>
            <option value="false" <%= !hero.fullTimeHero ? 'selected' : '' %>>No</option>
        </select>

        <label for="alignment">Alignment:</label>
        <select id="alignment" name="alignment" required>
            <option value="hero" <%= hero.alignment === 'hero' ? 'selected' : '' %>>Hero</option>
            <option value="villain" <%= hero.alignment === 'villain' ? 'selected' : '' %>>Villain</option>
            <option value="neutral" <%= hero.alignment === 'neutral' ? 'selected' : '' %>>Neutral</option>
        </select>

        <label for="powers">Powers (comma separated):</label>
        <input type="text" id="powers" name="powers" value="<%= hero.powers.join(', ') %>">

        <label for="associates">Associates (comma separated):</label>
        <input type="text" id="associates" name="associates" value="<%= hero.associates.join(', ') %>">

        <label for="enemies">Enemies (comma separated):</label>
        <input type="text" id="enemies" name="enemies" value="<%= hero.enemies.join(', ') %>">

        <label for="nemesis">Nemesis:</label>
        <input type="text" id="nemesis" name="nemesis" value="<%= hero.nemesis %>">

        <label for="citizenship">Citizenship:</label>
        <input type="text" id="citizenship" name="citizenship" value="<%= hero.citizenship %>" required>

        <label for="backgroundStory">Background Story:</label>
        <textarea id="backgroundStory" name="backgroundStory" required><%= hero.backgroundStory %></textarea>

        <button type="submit">Update Hero</button>
    </form>
</body>
</html>
